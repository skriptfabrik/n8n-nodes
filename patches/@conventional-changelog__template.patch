--- a/dist/elements.js
+++ b/dist/elements.js
@@ -54,7 +54,7 @@ export function list(array, callback) {
             ? `  ${line}`
             : line))
             .join('\n');
-        return rendered ? `* ${itemText}` : '';
+        return rendered ? `- ${itemText}` : '';
     });
 }
 /**
