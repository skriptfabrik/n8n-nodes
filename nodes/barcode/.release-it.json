{
  "$schema": "https://unpkg.com/release-it@19/schema/release-it.json",
  "git": {
    "changelog": "auto-changelog --unreleased-only --stdout",
    "commitMessage": "chore(release): barcode-${version}",
    "tagAnnotation": "chore(release): barcode-${version}",
    "tagName": "barcode-${version}",
    "requireCleanWorkingDir": false,
    "requireCommits": true
  },
  "github": {
    "release": true,
    "releaseName": "barcode ${version}"
  },
  "hooks": {
    "before:init": "turbo build",
    "after:bump": "auto-changelog --package"
  },
  "npm": {
    "skipChecks": true
  }
}
